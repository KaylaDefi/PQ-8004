[package]
name = "registry"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Agent identity registry (Layer 1) for the PQ-8004 trust stack"

[dependencies]
types = { path = "../types" }
pq_address = "0.1"
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
ml-dsa = { version = "0.0.4", features = ["rand_core"] }
