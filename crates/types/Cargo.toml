[package]
name = "types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared wire types for the PQ-8004 post-quantum agent payment protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
thiserror = "1"
